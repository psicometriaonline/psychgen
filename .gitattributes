# Force LF line endings on shell scripts and Dockerfiles so they run inside
# Linux containers regardless of the developer's host OS (Windows/macOS).
*.sh        text eol=lf
*.bash      text eol=lf
Dockerfile  text eol=lf
*.Dockerfile text eol=lf
entrypoint* text eol=lf
*.R         text eol=lf
